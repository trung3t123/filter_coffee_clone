import { StyleSheet, ViewStyle } from 'react-native';

export default StyleSheet.create({
  containerImage: {
    justifyContent: 'center',
    alignItems: 'center',
    flex: 1,
  } as ViewStyle,

  positionAbsolute: { position: 'absolute' } as ViewStyle,
});
